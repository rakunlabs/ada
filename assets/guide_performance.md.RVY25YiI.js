import{_ as e,c as a,o as s,a7 as i}from"./chunks/framework.BxaL7nSC.js";const k=JSON.parse('{"title":"Performance","description":"","frontmatter":{},"headers":[],"relativePath":"guide/performance.md","filePath":"guide/performance.md"}'),n={name:"guide/performance.md"};function r(o,t,d,l,h,c){return s(),a("div",null,[...t[0]||(t[0]=[i(`<h1 id="performance" tabindex="-1">Performance <a class="header-anchor" href="#performance" aria-label="Permalink to “Performance”">​</a></h1><p>Ada is designed for low overhead with zero heap allocations on the request hot path. This page documents benchmark results comparing ada against <a href="https://echo.labstack.com/" target="_blank" rel="noreferrer">Echo</a> and <a href="https://gin-gonic.com/" target="_blank" rel="noreferrer">Gin</a>.</p><h2 id="methodology" tabindex="-1">Methodology <a class="header-anchor" href="#methodology" aria-label="Permalink to “Methodology”">​</a></h2><p>All benchmarks use <code>httptest.NewRecorder</code> + <code>httptest.NewRequest</code> and call <code>ServeHTTP</code> directly — no TCP overhead, pure router + middleware performance. Each benchmark runs with <code>-benchmem -count=3</code> and reports the median result.</p><p>Environment: Go 1.26.2, Linux, AMD Ryzen 7 5800X, 16 logical CPUs. Framework versions: Echo v5.3.0 and Gin v1.12.0. Results will vary by hardware — run the benchmarks yourself for your specific environment.</p><p>Source code: <a href="https://github.com/rakunlabs/ada/tree/main/_examples/benchmark" target="_blank" rel="noreferrer"><code>_examples/benchmark/</code></a></p><h2 id="router-benchmarks" tabindex="-1">Router Benchmarks <a class="header-anchor" href="#router-benchmarks" aria-label="Permalink to “Router Benchmarks”">​</a></h2><h3 id="comparison-ada-vs-echo-vs-gin" tabindex="-1">Comparison: Ada vs Echo vs Gin <a class="header-anchor" href="#comparison-ada-vs-echo-vs-gin" aria-label="Permalink to “Comparison: Ada vs Echo vs Gin”">​</a></h3><table tabindex="0"><thead><tr><th>Benchmark</th><th>Ada</th><th>Echo</th><th>Gin</th></tr></thead><tbody><tr><td><strong>Static routes (5)</strong></td><td><strong>98.8 ns, 0 alloc</strong></td><td>201.7 ns, 0 alloc</td><td>171.6 ns, 0 alloc</td></tr><tr><td><strong>Static deep</strong> <code>/api/v1/users/list/all</code></td><td><strong>26.2 ns, 0 alloc</strong></td><td>49.7 ns, 0 alloc</td><td>35.0 ns, 0 alloc</td></tr><tr><td><strong>1 param</strong> <code>/users/{id}</code></td><td><strong>39.7 ns, 0 alloc</strong></td><td>43.8 ns, 0 alloc</td><td>41.3 ns, 0 alloc</td></tr><tr><td><strong>3 params</strong> <code>/api/{v}/users/{id}/posts/{pid}</code></td><td>100.9 ns, 0 alloc</td><td>66.3 ns, 0 alloc</td><td><strong>54.0 ns, 0 alloc</strong></td></tr><tr><td><strong>5 middlewares</strong></td><td><strong>33.4 ns, 0 alloc</strong></td><td>40.9 ns, 0 alloc</td><td>50.8 ns, 0 alloc</td></tr></tbody></table><h3 id="key-takeaways" tabindex="-1">Key Takeaways <a class="header-anchor" href="#key-takeaways" aria-label="Permalink to “Key Takeaways”">​</a></h3><p><strong>Routing</strong>: Ada uses a full-path compressed radix trie — &#39;/&#39; separators live inside the radix keys, so one comparison can match several path segments at once, and the &#39;/&#39; bookkeeping metadata for each key is pre-computed at registration time. This makes ada the fastest of the three on static routes and single-param routes. Param-heavy paths (3+ params) remain slower than Echo/Gin because each param requires a segment-boundary stop and a stdlib-compatible <code>SetPathValue</code> map write per param — the price of keeping handlers as plain <code>http.HandlerFunc</code> with <code>r.PathValue</code> support.</p><p><strong>Middleware</strong>: Ada&#39;s middleware chain is baked at registration time. All three frameworks complete the five-middleware benchmark without heap allocations. Ada is about <strong>18% faster than Echo v5</strong> and <strong>34% faster than Gin</strong> in this benchmark.</p><p><strong>Allocations</strong>: All three frameworks achieve zero heap allocations for routing. Ada uses in-place path walking (no <code>strings.Split</code>) and a sorted children slice (no Go map hashing).</p><p><strong>In practice</strong>: The routing differences (a few ns to roughly 47 ns in these scenarios) are negligible for real HTTP handlers that do 1-100 ms of actual work (database queries, API calls, JSON serialization). At 100k requests/second, the entire routing overhead is less than 1% of total CPU time. Router choice should therefore consider API design and operational features alongside these synthetic hot-path results.</p><h2 id="ada-only-detailed-benchmarks" tabindex="-1">Ada-Only Detailed Benchmarks <a class="header-anchor" href="#ada-only-detailed-benchmarks" aria-label="Permalink to “Ada-Only Detailed Benchmarks”">​</a></h2><table tabindex="0"><thead><tr><th>Benchmark</th><th>ns/op</th><th>B/op</th><th>allocs/op</th></tr></thead><tbody><tr><td>Static root <code>/</code></td><td>12.44</td><td>0</td><td>0</td></tr><tr><td>Static short <code>/users</code></td><td>18.90</td><td>0</td><td>0</td></tr><tr><td>Static deep <code>/api/v1/users/list/all</code></td><td>19.25</td><td>0</td><td>0</td></tr><tr><td>1 param <code>/users/{id}</code></td><td>39.06</td><td>0</td><td>0</td></tr><tr><td>3 params</td><td>97.29</td><td>0</td><td>0</td></tr><tr><td>Wildcard <code>/files/*</code></td><td>43.07</td><td>0</td><td>0</td></tr><tr><td>50 mixed routes</td><td>41.89</td><td>0</td><td>0</td></tr><tr><td>200 mixed routes</td><td>46.80</td><td>0</td><td>0</td></tr><tr><td>404 Not Found</td><td>218.1</td><td>98</td><td>3</td></tr><tr><td>405 Method Not Allowed</td><td>265.6</td><td>123</td><td>4</td></tr><tr><td>0 middlewares</td><td>18.81</td><td>0</td><td>0</td></tr><tr><td>1 middleware</td><td>20.57</td><td>0</td><td>0</td></tr><tr><td>5 middlewares</td><td>32.20</td><td>0</td><td>0</td></tr><tr><td>10 middlewares</td><td>45.74</td><td>0</td><td>0</td></tr><tr><td>Slot (runtime reload)</td><td>25.96</td><td>0</td><td>0</td></tr><tr><td>Pipeline (3 entries)</td><td>30.26</td><td>0</td><td>0</td></tr><tr><td>Pipeline (5 entries)</td><td>35.40</td><td>0</td><td>0</td></tr></tbody></table><h3 id="notes" tabindex="-1">Notes <a class="header-anchor" href="#notes" aria-label="Permalink to “Notes”">​</a></h3><ul><li><strong>Middleware scaling</strong>: 0 to 10 middlewares adds only ~27 ns because the chain is pre-built at registration time. The per-request cost is a function-call chain, not a loop.</li><li><strong>Route count scaling</strong>: 50 routes to 200 routes adds only ~5 ns due to the radix trie structure — lookup is O(path length), not O(route count).</li><li><strong>Slot / Pipeline overhead</strong>: ~3-5 ns over an equivalent static middleware. Both use pre-built handler chains with zero allocations. The only per-request cost is one atomic pointer load. When <code>WithTimeout</code> variants are active, one context derivation is added per request (~400 ns); this cost is only paid when timeout-based cancellation is in use.</li><li><strong>404/405 allocations</strong>: The remaining allocations on these paths come from stdlib <code>http.Error</code> / <code>http.NotFound</code> (header map write + body formatting). The middleware chain itself is pre-built at registration time and allocation-free per request.</li></ul><h2 id="optimizations" tabindex="-1">Optimizations <a class="header-anchor" href="#optimizations" aria-label="Permalink to “Optimizations”">​</a></h2><p>Ada&#39;s router achieves its performance through several key optimizations:</p><ul><li><strong>Full-path compressed radix trie</strong>: &#39;/&#39; separators are part of the radix keys, so consecutive static segments compress into a single key and one <code>memequal</code> comparison can consume several segments. Param/wildcard alternatives anchor at segment-start nodes and are only consulted on static dead ends.</li><li><strong>Sorted children slice</strong>: Trie child lookups use a sorted <code>[]staticChild</code> slice with linear scan instead of a Go <code>map[byte]*node</code>. For the typical 1-4 children per node, linear scan on contiguous memory (~0.5 ns) is significantly faster than Go map hashing (~8 ns).</li><li><strong>Inlined node structure</strong>: Static trie fields (<code>StaticKey</code>, <code>StaticChildren</code>) are inlined directly in the <code>node</code> struct, eliminating a pointer dereference per trie level and improving cache locality.</li><li><strong>Pre-computed key slash metadata</strong>: The position of the last &#39;/&#39; and the &#39;/&#39; count of each radix key are computed once at registration time and stored on the node, so per-request segment bookkeeping avoids <code>strings.LastIndexByte</code> / <code>strings.Count</code> on every key hop.</li><li><strong>In-place path walking</strong>: Request paths are walked byte-wise without allocating a <code>[]string</code> slice. Wildcard values are reconstructed via substring of the original path.</li><li><strong>Pre-chained error handlers</strong>: The 404/405 middleware chains are composed at registration time, not per request. The <code>Allow</code> header for 405/auto-OPTIONS responses is pre-computed on each node at registration.</li><li><strong>Pre-built middleware chains</strong>: Middleware is composed into a single handler closure at route registration time. Per-request cost is zero — no chain resolution, no allocation, no loop.</li><li><strong>Slice-based method dispatch</strong>: Per-node method handlers live in a small <code>[]methodEntry</code> slice scanned linearly instead of a <code>map[string]http.HandlerFunc</code>. For the typical 1-4 methods per node, a string comparison beats map hashing. The entry also carries the route pattern and pre-computed param names, so dispatch resolves handler, pattern, and params in a single lookup with no wrapper closure.</li><li><strong>Pre-built Slot/Pipeline chains</strong>: Both Slot and Pipeline pre-build handler chains at mutation time (not per-request). The hot path is one atomic pointer load (~1 ns) with zero allocations. Cancel contexts for <code>WithTimeout</code> variants are opt-in — only created when timeout-based cancellation is actually used.</li><li><strong>Leak-free context merging</strong>: When <code>WithTimeout</code> is active, <code>mergeContexts</code> returns a cleanup function that deregisters watchers from the generation context, preventing unbounded memory growth across requests.</li><li><strong>Direct method strings</strong>: HTTP methods from <code>net/http</code> are already uppercase per RFC 7230, so no <code>strings.ToUpper</code> conversion is needed.</li></ul><h2 id="running-benchmarks" tabindex="-1">Running Benchmarks <a class="header-anchor" href="#running-benchmarks" aria-label="Permalink to “Running Benchmarks”">​</a></h2><h3 id="ada-only-benchmarks" tabindex="-1">Ada-only benchmarks <a class="header-anchor" href="#ada-only-benchmarks" aria-label="Permalink to “Ada-only benchmarks”">​</a></h3><div class="language-sh"><button title="Copy Code" class="copy"></button><span class="lang">sh</span><pre class="shiki shiki-themes monokai monokai" style="--shiki-light:#F8F8F2;--shiki-dark:#F8F8F2;--shiki-light-bg:#272822;--shiki-dark-bg:#272822;" tabindex="0" dir="ltr"><code><span class="line"><span style="--shiki-light:#88846F;--shiki-dark:#88846F;"># From the repository root</span></span>
<span class="line"><span style="--shiki-light:#A6E22E;--shiki-dark:#A6E22E;">go</span><span style="--shiki-light:#E6DB74;--shiki-dark:#E6DB74;"> test</span><span style="--shiki-light:#AE81FF;--shiki-dark:#AE81FF;"> -bench=.</span><span style="--shiki-light:#AE81FF;--shiki-dark:#AE81FF;"> -benchmem</span><span style="--shiki-light:#AE81FF;--shiki-dark:#AE81FF;"> -count=3</span><span style="--shiki-light:#E6DB74;--shiki-dark:#E6DB74;"> .</span></span></code></pre></div><h3 id="framework-comparison" tabindex="-1">Framework comparison <a class="header-anchor" href="#framework-comparison" aria-label="Permalink to “Framework comparison”">​</a></h3><div class="language-sh"><button title="Copy Code" class="copy"></button><span class="lang">sh</span><pre class="shiki shiki-themes monokai monokai" style="--shiki-light:#F8F8F2;--shiki-dark:#F8F8F2;--shiki-light-bg:#272822;--shiki-dark-bg:#272822;" tabindex="0" dir="ltr"><code><span class="line"><span style="--shiki-light:#88846F;--shiki-dark:#88846F;"># From _examples/benchmark/</span></span>
<span class="line"><span style="--shiki-light:#A6E22E;--shiki-dark:#A6E22E;">go</span><span style="--shiki-light:#E6DB74;--shiki-dark:#E6DB74;"> test</span><span style="--shiki-light:#AE81FF;--shiki-dark:#AE81FF;"> -bench=.</span><span style="--shiki-light:#AE81FF;--shiki-dark:#AE81FF;"> -benchmem</span><span style="--shiki-light:#AE81FF;--shiki-dark:#AE81FF;"> -count=3</span><span style="--shiki-light:#E6DB74;--shiki-dark:#E6DB74;"> .</span></span></code></pre></div><h3 id="comparison-with-benchstat" tabindex="-1">Comparison with benchstat <a class="header-anchor" href="#comparison-with-benchstat" aria-label="Permalink to “Comparison with benchstat”">​</a></h3><p>For statistically rigorous comparison:</p><div class="language-sh"><button title="Copy Code" class="copy"></button><span class="lang">sh</span><pre class="shiki shiki-themes monokai monokai" style="--shiki-light:#F8F8F2;--shiki-dark:#F8F8F2;--shiki-light-bg:#272822;--shiki-dark-bg:#272822;" tabindex="0" dir="ltr"><code><span class="line"><span style="--shiki-light:#66D9EF;--shiki-dark:#66D9EF;">cd</span><span style="--shiki-light:#E6DB74;--shiki-dark:#E6DB74;"> _examples/benchmark</span></span>
<span class="line"><span style="--shiki-light:#A6E22E;--shiki-dark:#A6E22E;">go</span><span style="--shiki-light:#E6DB74;--shiki-dark:#E6DB74;"> test</span><span style="--shiki-light:#AE81FF;--shiki-dark:#AE81FF;"> -bench=BenchmarkAda</span><span style="--shiki-light:#AE81FF;--shiki-dark:#AE81FF;"> -benchmem</span><span style="--shiki-light:#AE81FF;--shiki-dark:#AE81FF;"> -count=10</span><span style="--shiki-light:#E6DB74;--shiki-dark:#E6DB74;"> .</span><span style="--shiki-light:#F92672;--shiki-dark:#F92672;"> &gt;</span><span style="--shiki-light:#E6DB74;--shiki-dark:#E6DB74;"> ada.txt</span></span>
<span class="line"><span style="--shiki-light:#A6E22E;--shiki-dark:#A6E22E;">go</span><span style="--shiki-light:#E6DB74;--shiki-dark:#E6DB74;"> test</span><span style="--shiki-light:#AE81FF;--shiki-dark:#AE81FF;"> -bench=BenchmarkEcho</span><span style="--shiki-light:#AE81FF;--shiki-dark:#AE81FF;"> -benchmem</span><span style="--shiki-light:#AE81FF;--shiki-dark:#AE81FF;"> -count=10</span><span style="--shiki-light:#E6DB74;--shiki-dark:#E6DB74;"> .</span><span style="--shiki-light:#F92672;--shiki-dark:#F92672;"> &gt;</span><span style="--shiki-light:#E6DB74;--shiki-dark:#E6DB74;"> echo.txt</span></span>
<span class="line"><span style="--shiki-light:#A6E22E;--shiki-dark:#A6E22E;">go</span><span style="--shiki-light:#E6DB74;--shiki-dark:#E6DB74;"> test</span><span style="--shiki-light:#AE81FF;--shiki-dark:#AE81FF;"> -bench=BenchmarkGin</span><span style="--shiki-light:#AE81FF;--shiki-dark:#AE81FF;"> -benchmem</span><span style="--shiki-light:#AE81FF;--shiki-dark:#AE81FF;"> -count=10</span><span style="--shiki-light:#E6DB74;--shiki-dark:#E6DB74;"> .</span><span style="--shiki-light:#F92672;--shiki-dark:#F92672;"> &gt;</span><span style="--shiki-light:#E6DB74;--shiki-dark:#E6DB74;"> gin.txt</span></span>
<span class="line"><span style="--shiki-light:#88846F;--shiki-dark:#88846F;"># Use benchstat to compare (go install golang.org/x/perf/cmd/benchstat@latest)</span></span></code></pre></div>`,29)])])}const m=e(n,[["render",r]]);export{k as __pageData,m as default};
