# Server

> https://developer.mozilla.org/en-US/docs/Web/HTTP/Reference/Headers/Server

This middleware adds the `Server` HTTP header to responses.

```go
mserver "github.com/rakunlabs/ada/middleware/server"
```
